<div class="comments">
  <h4>Berikan Komentar</h4>
  <%= form_tag do %>
    <div class="form-field">
      <%= text_area_tag(:comment) %>
    </div>

    <div class="form-field">
      <%= button_tag 'Komen', class: 'button button--sm button--pill', disabled: true %>
    </div>
  <% end %>
</div>
