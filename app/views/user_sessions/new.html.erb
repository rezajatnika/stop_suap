<div class="row">
  <div class="columns medium-4 medium-offset-4">
    <div class="login">
      <h3>Login to Stop Suap</h3>
      <%= form_for @user_session, url: login_path do |f| %>
        <div class="form-field">
          <%= f.text_field :email, placeholder: 'Email', autofocus: true %>
        </div>

        <div class="form-field">
          <%= f.password_field :password, placeholder: 'Password' %>
        </div>

        <div class="form-field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me do %>
            Remember me &middot;
          <% end %>
        </div>

        <%= f.submit 'Masuk', :class => 'button-form large float-center' %>
      <% end %>
      <hr>
      <p><%= link_to 'Lupa Password?', '#' %></p>
      <p>Belum Bergabung? <b><%= link_to 'Daftar StopSuap', signup_path %></b></p>
    </div>
  </div>
</div>
